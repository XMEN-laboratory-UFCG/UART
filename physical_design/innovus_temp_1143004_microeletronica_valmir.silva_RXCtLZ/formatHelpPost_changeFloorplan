.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH changeFloorplan  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBchangeFloorplan\fR \-  Changes the distances between the core box and the die box on the sides you specified. Use this command after specifying the floorplan data or running an initial floorplan
.SH Syntax \fBchangeFloorplan\fR  [-help]  [-noSnapToGrid]  [[-coreToLeft <value>][-coreToBottom <value>][-coreToRight <value>][-coreToTop <value>] |  [-coreToEdge {left bottom right top}]]
.P Changes the distances between the core box and the die box on the sides you specified. Use this command after specifying the floorplan data or running an initial floorplan. 
.SH Parameters   "\fB-help\fR" Outputs a brief description that includes the type and default information for each changeFloorplan parameter. For a detailed description of the command and all of its parameters, use the man command:  man changeFloorplan  "\fB-coreToBottom <value>\fR" Specifies the distance from the bottom of the core box to the bottom of the die box.  Data_type: float, optional  "\fB-coreToEdge {left bottom right top}   \fR" Specifies the distances between the core box and die box on each side (left, bottom, right, and top).  Data_type: box, optional  "\fB-coreToLeft <value>\fR" Specifies the distance from the left of the core box to the left of the die box.  Data_type: float, optional  "\fB-coreToRight <value>\fR" Specifies the distance from the right of the core box to the right of the die box.  Data_type: float, optional  "\fB-coreToTop <value>\fR" Specifies the distance from the top of the core box to the top of the die box.  Data_type: float, optional  "\fB-noSnapToGrid\fR" Specifies that the core box (or die box) boundary will not be snapped to the nearest metal pitch even if the specified die/ core box size is not an integer multiple of the smallest metal pitch. This prevents the input floorplan boxes from being changed.  Data_type: bool, optional 
.SH Example
.RS  "*" 2 The following command changes the distance from the bottom of the core box to the bottom of the die box to 200:  changeFloorplan -coreToBottom 200
.RE 
.SH Related Information
.RS  "*" 2 saveDesign  "*" 2 saveFPlan  "*" 2 Floorplanning the Design chapter in the Innovus User Guide
.RE
.P
