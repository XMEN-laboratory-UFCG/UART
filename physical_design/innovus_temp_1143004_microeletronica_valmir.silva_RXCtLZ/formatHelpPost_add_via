.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH add_via  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBadd_via\fR \-  Adds a new special-route via instance to the design
.SH Syntax \fBadd_via\fR  [-help]  [-net <net>]   -pt {<x y>}   [-shape {blockring | blockagewire | blockwire | corewire | drcfill | fillopc | fillwire | followpin | iowire | none | padring | ring | stripe}]   [-shield_net <net>]   [-status {routed | fixed | cover | shield}]  [-user_class <value>]   -via <via_def>
.P Adds a new special-route via instance to the design. A DB pointer is returned to the via, if successful. You can use the command at any point in the design flow. To add regular route via, see editAddVia. 
.SH Parameters   "\fB-help\fR" Outputs a brief description that includes the type and default information for each add_via parameter. For a detailed description of the command and   all of parameters, use the man command: man add_via  "\fB-net <net>\fR" Specifies the name or pointer of the net to associate with the via instance.   If not specified, the via is assigned as a floating fillwire via, unless -shape is specified.  "\fB-pt {<x y>}\fR" Specify the placement location (in microns) of the via instance.  "\fB-shape {blockring | blockagewire | blockwire | corewire | drcfill | fillopc | fillwire | followpin | iowire | none | padring | ring | stripe}\fR" Specifies the special route shape (DEF SPECIALNETS wiring+ SHAPE).  If -net is specified, the default shape is none. If -net is not specified, the default shape is fillwire.   "\fB-shield_net <net>\fR" Specifies the name or pointer of the net to be shielded by the via instance.   Only valid when -status is set to shield.   "\fB-status {routed | fixed | cover | shield}\fR" Specifies the special route (DEF SPECIALNETS) wiring status.  Default: routed  "\fB-user_class <value>\fR" Specifies the user-defined string to be added to the via instance that is created.   "\fB-via <via_def>\fR" Specify the via-master name or pointer of the via-master to place. 
.SH Examples
.RS  "*" 2 The following command adds a  via with via-master name of my_via1:  add_via -via my_via1 -pt {100.1 200.5} -net VSS -shape followpin  "*" 2 The following command adds a VSS via (via cell VIA2X) that shields reset net:  add_via -via VIA2X -pt {13.0 145.5} -net VSS -shield_net reset -status shield  "*" 2 To add a parameterized via you can look up the via-master with add_via_definition (in this case a 1x2 cut via with all the other parameters the default value).   It will either return an existing via-master with the same parameters, or create it.  Then use it to create an instance of the via.  set myvia [add_via_definition -via_rule via1_rule -row_col {1 2}]  add_via -via $myvia -pt {10.5 10.5} -net VS 
.RE
.P
