.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH editChangeVia  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBeditChangeVia\fR \-  Replaces the selected vias or the vias listed in -from with the specified vias or with vias having the specified cut class and pattern
.SH Syntax \fBeditChangeVia\fR  [-help]   [-net <net_name>]  [-signal_only {0 | 1}]  {[-from <string> [-area {<x1 y1 x2 y2>} | -at {<x y>} ]] | [-selected]}  {-to <string> | [-via_columns <integer> -via_rows <integer> [-cut_class <class_name>]]}
.P Replaces the selected vias or the vias listed in -from with the specified vias or with vias having the specified cut class and pattern.
.P While modifying a via with editChangeVia, the setEditMode -drc_on and -keep_via settings are used in combination to decide whether or not a via should be replaced if the new via cell results in DRC violations:
.RS  "*" 2 setEditMode -drc_on false  With this setting, the tool does not check for Design Rule Check (DRC) violations when you modify a via using the editChangeVia command.
.RE
.RS  "*" 2 setEditMode -drc_on true -keep_via false  With this setting, the tool checks for DRC violations and allows you to replace the via even if the new via is generating violations. The DRC violations are displayed on the layout.
.RE
.RS  "*" 2 setEditMode -drc_on true -keep_via true  With this setting, the tool checks for DRC violations and retains the original via if the new one is generating violations.
.RE
.P The behavior defined above applies for all via modifications with editChangeVia, irrespective of whether you use the -to <string> option or the -via_columns <integer> -via_rows <integer> options.
.P Note that the setEditMode -drc_on true option does not check cut spacing, enclosure spacing, or other geometry of the via cell itself. 
.SH Parameters   "\fB-help\fR" Prints a brief description that includes type and default information for each editChangeVia parameter.  For a detailed description of the command and all of its parameters, use the man command:  man editChangeVia  "\fB-area {<x1 y1 x2 y2>}\fR" Specifies the coordinates of the area in which vias are to be replaced. If a via indicated by the -from parameter is in this area, replaces the via with the via indicated by the -to parameter.  "\fB-at {<x y>}\fR" Specifies the coordinates for the origin of the via to be replaced. The via at this location is replaced by the via indicated by the -to parameter.  "\fB-cut_class <class_name>\fR" Specifies the cut class of the replacement vias.   "\fB-from <string>\fR" Specifies a via or list of vias to replace. Enclose the via names on the list in curly braces and separate each name with a space. Vias whose masters are not specified in string are not changed.  Notes: 
.RS  "*" 2 You must specify the -from or the -selected parameter. If you do not specify at least one of these parameters, the software does not change any vias.  "*" 2 The number of via names on the -from list must be equal to the number of via names on the -to list. In addition, the corresponding pair of vias in each list must be on the same layer.
.RE   "\fB-net <net_name>\fR" Specifies the net name on which to change vias.  "\fB-selected\fR" Replace vias for selected ones only.  Note: You must specify the -from or the -selected parameter. If you do not specify at least one of these parameters, the software does not change any vias.  "\fB-signal_only {1 | 0}\fR" Specifies whether signal vias only or signal and special vias are changed. Specify 1 to change signal via only. Specify 0 to change signal and special vias.  Default: 0  "\fB-to <string>\fR" Specifies a via or a list of vias to replace the vias indicated by the -from parameter. If you specify more than one via, enclose the via list in curly braces and separate each via name with a space.  Note: The number of via names on the list must be the equal to the number of via names on the list for the -from parameter. In addition, the corresponding pair of vias in each list must be on the same layer.  "\fB-via_columns <integer>\fR" Specifies the number of columns in the replacement vias.   "\fB-via_rows <integer>\fR" Specifies the number of rows in the replacement vias. 
.SH Examples
.RS  "*" 2 The following command changes all VIA_XX vias of the net VDD1 located within a specified region to VIA_YY vias:   editChangeVia -net VDD1 -area {x1 y1 x2 y2} -from VIA_XX -to VIA_YY  "*" 2  The following command replaces the selected vias with vias having a cut pattern of 3 by 3:  editChangeVia -selected -via_columns 3 -via_rows 3
.RE 
.SH Related Information
.RS  "*" 2 editChangeLayer  "*" 2 editChangeMask  "*" 2 editChangeNet  "*" 2 editChangeRule  "*" 2 editChangeStatus  "*" 2 editDelete  "*" 2 editSelect  "*" 2 getEditMode  "*" 2 redo  "*" 2 setEditMode  "*" 2 undo  "*" 2 Editing Wires chapter in the User Guide  "*" 2 Edit Menu chapter in the Menu Reference 
.RS  "*" 2 Wire section
.RE 
.RE
.P
