.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH replace_proto_model  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBreplace_proto_model\fR \-  Converts flexmodel netlist back to the original full netlist with changes at the partition block level whenever feedthrough insertion step is needed in the flow
.SH Syntax \fBreplace_proto_model\fR  [-help]  [-out_dir <string]>  [-ptn_dir <string>]  [-module_model_tag <string> -full_module_model_tag<string>]
.P Converts flexmodel netlist back to the original full netlist with changes at the partition block level whenever feedthrough insertion step is needed in the flow. You can use this command after using the savePartition command. This command saves a directory under each partition directory with the default name <designName>_proto_full.enc.dat and you need to use this DB for block implementation.
.P To use this command in a FlexModel based partition flow:
.RS  "*" 2 Import a full chip FlexModel netlist design.  "*" 2 Floorplan the design.  "*" 2 Run the following commands to analyze the floorplan. 
.RS  "*" 2 place_design  "*" 2 earlyGlobalRoute  "*" 2 timeDesign -proto  "*" 2 load_timing_debug_report -proto
.RE  "*" 2 Do feedthrough insertion
.  "*" 2 Do pin assignment  "*" 2 Optionally optimize the design.  "*" 2 Budget the timing for blocks using the deriveTimingBudget command.  "*" 2 Partition the design using the partition command.  "*" 2 Save the partition using savePartition -dir $ptnDir (for placement and pin locations copy)  "*" 2 replace_proto_model -ptn_dir $ptnDir -outdir $ptnDir
.RE
.P Note: The replace_proto_model command only supports the netlist changes caused by feedthrough insertion, so replace_proto_model will have issues if there is some netlist change issued by other commands such ascreateLogicHierarchy before calling replace_proto_model. This is a limitation for the replace_proto_model command.  
.SH Parameters   "\fB-help\fR" Outputs a brief description that includes type and default information for each replace_proto_model parameter.  For a detailed description of the command and all of its parameters, use the man command:   man replace_proto_model.  "\fB-full_module_model_tag< string>\fR" Specifies the module model tag for the original full netlist that is used in the iHDB directory structure for saving the full hierarchical DB (output) after eco.   Note: The -full_module_model_tag and  -module_model_tag parameters are part of the Integrated Hierarchical Database (iHDB) flow and are used by the replace_proto_model command to convert the FlexModel netlist back to the original full netlist in the iHDB directory structure. Before using these parameters, you must set the default central directory name (set_module_model -default_dir <default_model_data_directory> ) where the module models data will be saved/loaded.  Data_type: string, optional  "\fB-module_model_tag <string>\fR" Specifies the module model tag for the hierarchical DB (input). This module model tag is used in the Integrated Hierarchical Database flow.  Note: The -full_module_model_tag and  -module_model_tag parameters are part of the iHDB flow and are used by the replace_proto_model command to convert the FlexModel netlist back to the original full netlist in the iHDB directory structure. Before using these parameters, you must set the default central directory name (set_module_model -default_dir <default_model_data_directory> ) where the module models data will be saved/loaded.  Data_type: string, optional  "\fB-out_dir <string>\fR" Specifies the directory to save post ECO full top/block netlist partition data.  Data_type: string, optional  "\fB-ptn_dir <string>\fR" Specifies the partition directory for original full netlist.  Data_type: string, optional 
.SH Example
.P The following command will read the FlexModel data with tag name ptn as input, and dump the original full netlist with tag name full in the iHDB directory structure. 
.P replace_proto_mode  -module_model_tag ptn -full_module_model_tag full
.P (<Image> To view the image, refer to the Innovus Text Command Reference - Release 22.1 manual.) 
.SH Related Information
.RS  "*" 2  savePartition   "*" 2 set_proto_model  "*" 2  Prototyping Methodologies chapter in the Innovus User Guide
.RE
.P
