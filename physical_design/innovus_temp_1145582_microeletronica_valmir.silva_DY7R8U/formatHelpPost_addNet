.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH addNet  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBaddNet\fR \-  Adds a net to the design
.SH Syntax \fBaddNet\fR  [-help]  <netName>  [-bus <startID>:<endID>]  [-moduleBased <verilogModule>]   [-power  | -ground ] 
.P Adds a net to the design. The net can be logical or physical. 
.SH Parameters    "\fB-help\fR" Outputs a brief description that includes the type and default information for each addNet parameter.  For a detailed description of the command and all of its parameters, use the  man  command:    man addNet  "\fB-bus <startID>:<endID>\fR" Specifies the bit range for bus nets. The <startID> and <endID> indicate the first and last bits on the bus. You must separate the start and end IDs with a colon (:).  If the specified bus does not exist, tool will create a bussed Verilog net.  If the specified bus already exists, tool will do bus net resizing. The specified bus bit range must be adjacent to the first or last bit of the existing bus. Also, it must have the same ascending or descending order as existing bus.  Note: PG bus net is not supported  "\fB  -ground\fR" Adds a ground net.   "\fB-moduleBased <verilogModule>\fR" Adds the new net to the specified verilog module.  If the module name contains special characters, you need to start the module name with a backslash '\\' and end it with a space.  "\fB<netName>\fR" Specifies the net to add.  If the net name contains special characters, you need to start the part of the net name, which contains special characters, with a backslash '\\' and end it with a space.  Note: You must specify <netName> before you specify other parameters. The software issues an error message if you do not specify the <netName> first.  "\fB-power\fR" Adds a power net. 
.SH Examples
.RS  "*" 2 The following command adds net i1/n1:   addNet i1/n1
.RE
.RS  "*" 2  The following command adds one bit i1/n1[6] to existing bus i1/n1[2:5]:  addNet i1/n1 -bus 6:6  "*" 2  The following command adds three bits i1/n1[-1], i1/n1[0], i1/n1[1] to existing bus i1/n1[2:5]:  addNet i1/n1 -bus -1:1
.RE
.RS  "*" 2 The following command adds net with base name ant_net to hierarchy DTMF_INST/TDSP_CORE_INST/TDSP_CORE_MACH_INST[0].g:  addNet {DTMF_INST/TDSP_CORE_INST/\\TDSP_CORE_MACH_INST[0].g /ant_net}  Here the "[" and "]" brackets between the backslash and the space are special characters.
.RE
.RS  "*" 2 The following command adds net with base name TDSP_CORE_INST/TDSP_CORE_MATCH_INST[0].g/ant_net to hierarchy DTMF_INST:  addNet {DTMF_INST/\\TDSP_CORE_INST/TDSP_CORE_MATCH_INST[0].g /ant_net }   "*" 2 The following command adds net with base name ant_net[1] to module H[1]:  addNet {\\ant_net[1] } -moduleBased {\\H[1] }
.RE 
.SH Related Information
.RS  "*" 2 deleteNet
.RE
.P
