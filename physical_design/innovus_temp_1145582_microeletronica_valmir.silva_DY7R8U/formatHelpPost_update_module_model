.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH update_module_model  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBupdate_module_model\fR \-  Invokes an independent session in interactive or batch mode at top-level design
.SH Syntax \fBupdate_module_model\fR  [-help]   [-cpu <number_of_cpus>]   [-distribute_job]   [-eco_report]   [-log_dir <dir_name>]  [-return_tag <tag_name>]  [-return_type {lef flexIlm ilm pnr}]   [-tag <tag_name>]  {[-distribute_run | -cell <cell_name>] [-interactive | -plugin_tcl <tcl_commands> | -gen_eco_only ]} 
.P Invokes an independent session in interactive or batch mode at top-level design. The update_module_model command can be called at top-level design. 
.P Note: When an independent session is invoked, the license(s) will be checked-out from the main Innovus session and checked in to the independent session. Once you exit the independent session, the license(s) will return back to main Innovus session.
.P The update_module_model command:
.RS  "*" 2 automatically updates the changes from top to block in an independent session.  "*" 2 runs the user-provided plug-in commands in batch mode.  "*" 2 automatically brings back the updated LEF models of the same tag to the top-level once the job is done.
.RE
.P Note: You should run the create_module_model command to save different types of models at the end of an independent session. 
.SH Parameters    "\fB-cell <cell_name>\fR" Specifies which cell the command will update changes.  Data_type: string, optional  "\fB-cpu <number_of_cpus>\fR" Specifies the number of CPUs that will be used for the independent session. By default it uses the same of number of CPUs as its parent.  Data_type: int, optional  "\fB-distribute_job\fR" Enables distributed processing where more than one block design can be run in parallel.  Data_type: bool, optional  "\fB-distribute_run\fR" Enables you to execute all the specified update_module_model distributed jobs using a Makefile.  Data_type: bool, optional   "\fB-eco_report  \fR" Specifies the output report file that contains the ECO change information.  Data_type: bool, optional  "\fB-gen_eco_only\fR" Generates an eco file that contains general netlist ECO and placement changes. This mode allows you to explicitly review the ECO before applying it to the block DB.  (<Image> To view the image, refer to the Innovus Text Command Reference - Release 22.1 manual.)   Data_type: bool, optional  "\fB-help\fR" Outputs a brief description that includes the type and default information for each update_module_model parameter. For a detailed description of the command and all of its parameters, use the man command:   man update_module_model  "\fB-interactive  \fR" Invokes independent session in interactive mode.  Data_type: bool, optional  "\fB-log_dir <dir_name>\fR" Specifies the log file directory name.  Data_type: string, optional  "\fB-plugin_tcl <tcl_commands>  \fR" Specifies the TCL commands to be run in a batch independent session.  Data_type: string, optional  "\fB-return_tag <tag_name>\fR" Specifies the tag name that will be used to update the specified cell model at the top-level design.  Default: The default return tag is the tag name of the current loaded block design.  Data_type: string, optional  "\fB-return_type {flexIlm ilm lef pnr}\fR" Specifies the model type to be brought back to top-level design. The return type can be lef, flexIlm, ilm, or pnr.  Default: lef  Data_type: enum, optional  "\fB-tag <tag_name>\fR" Allows you to explicitly specify the tag name of the cell pnr model that will be loaded. If a tag is not specified, the update_module_model command loads the pnr model of the same tag with the currently loaded design (restore_module_model).  For example, if the currently loaded design has tag init, then the update_module_model command will load the pnr model of tag init for the specified cell. It will display an error if the tag is not available.  Data_type: string, optional 
.SH Example
.RS  "*" 2  The following command opens an independent session in batch mode for the full DB of the cell tdsp_core, tag preCts:  update_module_model -cell tdsp_core -return_type ilm -plugin_tcl {   route_eco   create_module_model  }
.RE 
.SH Related Information
.RS  "*" 2 check_module_model  "*" 2 commit_module_model  "*" 2 create_module_model  "*" 2 report_module_model  "*" 2 restore_module_model  "*" 2 savePartition  "*" 2 set_module_model  "*" 2 write_module_model_context  "*" 2 Hierarchical and Prototyping Flow chapter in the Innovus User Guide
.RS  "*" 2  Integrated Hierarchical Database Flow
.RE 
.RE
.P
