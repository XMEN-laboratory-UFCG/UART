.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH pack_module_model_libs  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBpack_module_model_libs\fR \-  Packs libraries for the Integrated Hierarchical Database (iHDB) flow designs
.SH Syntax \fBpack_module_model_libs\fR  [-help]   [-cell <cellName>]   [-validate]
.P Packs libraries for the Integrated Hierarchical Database (iHDB) flow designs.
.P Notes: 
.RS  "*" 2 You must specify the default central directory name (set_module_model -default_dir), before using this command. In case default central directory is moved to a different location, the libraries can be packed again.  "*" 2 The packed libraries are saved at <default_model_data_directory>/<cell>/.libs.
.RE 
.SH Parameters    "\fB-cell <cellName>\fR" Specifies the cell name. All pnr model libraries of all tags under the specified cell will be linked to the packed libraries (
.libs) saved by the pack_module_model_libs command.  Note: The packed libraries are saved at <default_model_data_directory>/<cell>/.libs.  Data_type: string, optional  "\fB-help\fR" Outputs a brief description that includes the type and default information for each pack_module_model_libs parameter. For a detailed description of the command and all of its parameters, use the man command:   man pack_module_model_libs  "\fB-validate\fR" Compares the checksum before and after packing libraries. It checks whether the libraries before and after packing are the same.  Note: When the -validate parameter is specified, the pack_module_model_libs command will output after_pack_libs.check_sum and before_pack_libs.check_sum under the
.libs directory for comparison.  Data_type: bool, optional 
.SH Example
.RS  "*" 2 The following command specifies the default central directory name (./DATA) where models data will be saved/loaded and then specifies the cell name. All pnr model libraries of all tags under the specified cell will be linked to the packed libraries (
.libs) saved by the pack_module_model_libs command.  set_module_model -default_dir DATA2  pack_module_model_libs -cell tdsp_core -validate  Output:  This command will output the packed libraries at <DATA2/tdsp_core/.libs>. It will also create <after_pack_libs.check_sum> and<before_pack_libs.check_sum> under the <.libs> directory for comparison.
.RE 
.SH Related Information
.RS  "*" 2 set_module_model  "*" 2 Hierarchical and Prototyping Flow chapter in the Innovus User Guide
.RS  "*" 2  Integrated Hierarchical Database Flow
.RE 
.RE
.P
