.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH set_visible_bumps  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBset_visible_bumps\fR \-  Displays only the bumps with the specified bump cell names and hides all other bumps
.SH Syntax \fBset_visible_bumps\fR  [-help]  {-bump_cells <bump_cell_name>| -all} 
.P Displays only the bumps with the specified bump cell names and hides all other bumps.
.P To redisplay all bumps, specify the -all parameter. 
.SH Parameters   "\fB-help\fR" Prints a brief description that includes type and default information for each set_visible_bumps parameter.  For a detailed description of the command and all of its parameters, use the man command:   man set_visible_bumps  "\fB-all\fR" Displays all bumps.  Note: By default, all bumps are displayed. When you specify set_visible_bumps <bump_cell_name>, the tool displays only the bumps with the specified bump cell names and hides all other bumps. You can then use set_visible_bumps <-all> to redisplay all bumps.   "\fB-bump_cells <names>\fR" Displays only the bumps with the specified bump cell names and hides all other bumps. You can specify multiple bump cell names. 
.SH Examples
.RS  "*" 2 The following command displays only the bumps with bump cell as BUMPCELL:  set_visible_bumps -bump_cells BUMPCELL  "*" 2 The following command redisplays all bumps:  set_visible_bumps -all
.RE 
.SH Related Information
.RS  "*" 2 get_visible_bumps
.RE
.P
