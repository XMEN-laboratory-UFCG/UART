.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH reportDanglingPort  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBreportDanglingPort\fR \-  Reports ports that are disconnected from both the inside and outside of a module, and ports that are connected within the module but disconnected outside the module
.SH Syntax \fBreportDanglingPort\fR  -help  [-ptn]  -outfile <fileName> 
.P Reports ports that are disconnected from both the inside and outside of a module, and ports that are connected within the module but disconnected outside the module.
.P Note: If the design has some dangling or unconnected ports, Innovus will add FE_UNCONNECTED_* nets for these dangling or unconnected ports in the design during optimization. You can report these dangling ports using the reportDanglingPort command. 
.SH Parameters    "\fB-help\fR" Prints a brief description that includes type and default information for each reportDanglingPort parameter.  For a detailed description of the command and all of its parameters, use the man command:  man reportDanglingPort  "\fB-outfile\fR" Specifies the name of the report generated by this command.  Data_type: string, required  "\fB-ptn\fR" Specifies a partition only flag.  Data_type: bool, optional 
.SH Example
.P The following command writes information on dangling reports to file myreport:
.P reportDanglingPort -outfile myreport  
.SH Related Information
.RS  "*" 2 deleteDanglingPort  "*" 2 disconnectDanglingPort
.RE
.P
