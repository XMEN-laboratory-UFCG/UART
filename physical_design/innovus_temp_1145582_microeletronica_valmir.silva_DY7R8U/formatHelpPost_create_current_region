.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH create_current_region  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBcreate_current_region\fR \-  Specifies static or dynamic current regions on global power-grids over the placed macro to accurately capture high and low current consuming regions during rail analysis
.SH Syntax  \fBcreate_current_region\fR   [-help]   -reset | {-region <x1 y1 x2 y2>  [-current <value>| -current_pwl_file<filename>]   [-die_instance_name <dieinstname>]  [-file <filename>]  [-layer <layername>]   [-intrinsic_cap <value>]   [-loading_cap <value>]   [-series_resistance<value>]  [-net<net_name>]  [-polygon {<x1> <y1> <x2> <y2> <x3> <y3>
..<x1> <y1>}]   [-pwrnet<net_name>]  [-gndnet<net_name>]  } 
.P Specifies static or dynamic current regions on global power-grids over the placed macro to accurately capture high and low current consuming regions during rail analysis.
.P You can use this command prior to rail analysis to specify current regions or power distribution over the top of power-grid on a user-defined layer. These current regions are generally specified for a placed macro for which power-grid view library is not available, and power/current distribution inside this macro could possibly impact IRdrop in neighboring regions. These current regions can also be defined over top-level power-grid for an unplaced macro during early stages of power-planning. 
.SH Parameters    "\fB-current <value>\fR" In the static mode (set_rail_analysis_mode -method static), the parameter specifies a single current value in mAmp.  In the dynamic mode (set_rail_analysis_mode -method dynamic) the parameter specifies a PWL current waveform in time (in ns) and current (in mA) format. For example, 0ns 0mA 0.5ns 1mA 1ns 2mA 3ns 1mA 4ns 0.  "\fB-current_pwl_file<filename>\fR" Specifies a PWL current file. The file has two columns, time (in ns) and current value (in mA), as shown below:  time1 value1   time2 value2  The  -current_pwl_file and  -current parameters are mutually exclusive.   "\fB-die_instance_name <dieinstname>\fR" Specifies the name of the die to which you need to apply a current region. This parameter is only applicable to the multi-die analysis flow (set_rail_analysis_mode -die_mode multi-die).  The parameter allows you to accurately assign current to power and ground nets in specific geometric regions for individual dies during rail analysis in 3D-IC designs.  "\fB-file <filename>\fR" Specifies the name of a file containing the list of current regions for each die. -file can be used only with the  -die_instance_name parameter. The other parameters of the  create_current_region command can be included within the specified file.   Example:  create_current_region -file <create_current_region_die0.tcl> -die_instance_name Die0  create_current_region -file <create_current_region_die1.tcl> -die_instance_name Die1  ###File content in <create_current_region_die0.tcl>  -net VDD -current  50mA -layer M2 -region "10 10 100 100"  -net VDD -current  50mA -layer M2 -region "100 100 200 200"    -net VSS -current  100mA -layer M1 -region "10 10 200 200"  ###File content in <create_current_region_die1.tcl>  -net VDD -current  150mA -layer M2 -region "10 10 50 50"    -net VDD -current  50mA -layer M2 -region "100 100 200 200"    -net VSS -current  200mA -layer M1 -region "10 10 200 200"  "\fB-help\fR" Outputs a brief description that includes the type and default information for each create_current_region parameter.  For a detailed description of the command and all of its parameters, use the man command man create_current_region.  "\fB-gndnet<net_name>\fR" Specifies a ground net name on which the specified current is to be applied.   "\fB-intrinsic_cap <value>\fR" Specifies the cell intrinsic capacitance in pico farads (pf). This is required parameter when set in the dynamic mode (set_rail_analysis_mode -method dynamic). This parameter is disabled in static mode.  "\fB-layer <layername>\fR" Specifies a layer name on which the scaling will be performed.  Multiple commands can be issued to scale resistance on multiple layers. This is a required parameter.  "\fB-loading_cap value\fR" Specifies the cell loading capacitance in pico farads (pf). This is required parameter when set in the dynamic mode (set_rail_analysis_mode -method dynamic). This parameter is disabled in static mode.  "\fB-net<net_name>\fR" Specifies a net name on which the specified current is to be applied.   "\fB-polygon {<x1> <y1> <x2> <y2> <x3> <y3>
..<x1> <y1>}\fR" Specifies a rectilinear current region where the current will be distributed within. The rectilinear region enables you to specify multiple x,y points to add current regions in the areas that are not rectangular in shape.  When specified, the last xy coordinate location will be the same as the first xy location. The xy coordinates will be in the counterclockwise sequence.  "\fB-pwrnet<net_name>\fR" Specifies a power net name on which the specified current is to be applied.   "\fB-region <x1 y1 x2 y2>\fR" Specifies the region on the layer across which the specified current and capacitance (in dynamic mode) is distributed by the rail analysis engine. The units of x and y should be in microns.  This is a required parameter.  "\fB-reset\fR" Resets all the previous create_current_region command settings.  "\fB-series_resistance<value>\fR" Specifies the series resistance (ESR) value attached to each tap point in the current region definition. 
.SH Examples
.RS  "*" 2 The following command specifies static current region:   create_current_region -current 2mA -layer METAL_1 -region "100 100 200 200" 
.RE
.RS  "*" 2 The following command specifies dynamic current region:   create_current_region -current {0ns 0mA 0.5ns 1mA 1ns 2mA 3ns 1mA 4ns 0} -layer METAL_1 -region "200 200 400 400" -intrinsic_cap 40 -loading_cap 20 
.RE
.RS  "*" 2 The following command resets all create_current_region parameters to their default values:   create_current_region -reset  "*" 2 The following command specifies rectilinear current regions for die instance Die1:   create_current_region -net VDD -current 50mA -layer Metal5 -polygon "10 10 100 10 100 100 10 100 10 10 " -die_instance_name Die1  create_current_region -net VDD -current 50mA -layer Metal5 -polygon "100 100 200 100 200 200 100 200 100 100" -die_instance_name Die1
.RE 
.SH Related Information
.RS  "*" 2 scale_what_if_capacitance  "*" 2 scale_what_if_resistance  "*" 2 scale_what_if_current  "*" 2 "What-If Rail Analysis" in the Voltus IC Power Integrity Solution User Guide
.RE
.P
