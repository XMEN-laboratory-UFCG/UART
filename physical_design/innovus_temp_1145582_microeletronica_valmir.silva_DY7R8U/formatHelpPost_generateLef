.nf
.P Product Version     22.14    Cadence Design Systems, Inc. 
.fi
.TH generateLef  22.14 "Fri Oct 06 02:12:27 2023" 
.SH Name \fBgenerateLef\fR \-  Uses input data to create an optimized LEF file with generated vias for the NanoRoute router
.SH Syntax \fBgenerateLef\fR  [-help]   <outputLefFileName>   [-clfFile <clfFileName>]  [-lefFile <lefFileName>]  [-lefFileList {<technology.lef> <cell1.lef> <cell2.lef>
...}]  [-noWE]  [-techFile <technologyFileName>]
.P Uses input data to create an optimized LEF file with generated vias for the NanoRoute router.
.P Input data can include the following:
.RS  "*" 2 Original or customized LEF technology files
.RE
.RS  "*" 2 Milkyway technology file
.RE
.RS  "*" 2 Milkyway CLF file
.RE
.P Note: Include sample vias for each cut layer in the LEF file. You must also include test via rules in the input in order for the output to contain the created vias.
.RS  "*" 2 Scans default vias from input data to determine the following:
.RE
.RS
.RS  "*" 2 Minimum overhang
.RE 
.RE
.RS
.RS  "*" 2 Width
.RE 
.RE
.RS
.RS  "*" 2 Spacing
.RE 
.RE
.RS
.RS  "*" 2 End-of-line overhang
.RE 
.RE
.RS  "*" 2 Generates auto-vias for default, power, and other generated explicit vias.
.RE
.RS  "*" 2 Retains original naming convention for LEF macro reference. If there is a conflict, utilizes original names.
.RE
.RS  "*" 2 Updates technology description with all vias (for example, adds multi-cut vias automatically if a MINIMUMCUT statement is provided).
.RE
.P Use the generateLef command before routing with the NanoRoute router. 
.SH Parameters   "\fB-help\fR" Prints a brief description that includes the type and default information for each generateLef parameter.  For a detailed description of the command and all of its parameters, use the man command:   man generateLef.  "\fB-clfFile <clfFileName>\fR" Specifies a Milkyway CLF input file.  You must also specify the -techFile parameter when you specify this parameter.  "\fB-lefFile <lefFileName>\fR" Specifies a LEF input file.  "\fB-lefFileList {<technology.lef cell1.lef cell2.lef
...>}\fR" Specifies a list of LEF files to read into the software to use for generating vias with different orientations, extensions, and offsets. (<Image> To view the image, refer to the Innovus Text Command Reference - Release 22.1 manual.) The file can list only one filename per line. The first file listed must be the technology LEF file.   "\fB-noWE\fR" Generates default vias for no wire extension routing.   "\fB<outputLefFileName>\fR" Specifies the new LEF file to output.  "\fB-techFile <technologyFileName>\fR" Specifies a Milkyway technology file.  You must also specify the -clfFile parameter when you specify this parameter. 
.SH Examples
.RS  "*" 2 The following command converts an old LEF file to an optimized LEF file for NanoRoute:   generateLef -lefFile old.lef new.lef
.RE
.RS  "*" 2 The following command converts a Milkyway technology file to an optimized LEF file for NanoRoute:   generateLef -techFile mw.tf mw.lef
.RE
.RS  "*" 2 The following command converts a CLF file to an optimized antenna LEF file for NanoRoute:   generateLef -clfFile mw.clf ant.lef
.RE 
.SH Related Information
.RS  "*" 2 Using the NanoRoute Router chapter in the Innovus User Guide
.RE
.RS
.RS  "*" 2 "Running the NanoRoute Router with the Innovus Text Commands"
.RE 
.RE
.RS
.RS  "*" 2 "Using a Design Flow that Includes Astro or Apollo"
.RE 
.RE
.P
